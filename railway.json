{
    "$schema": "https://railway.app/railway.schema.json",
    "deploy": {
        "startCommand": "php bin/console messenger:setup-transports --no-interaction && php bin/console doctrine:migrations:migrate --no-interaction && php -d variables_order=EGPCS -S 0.0.0.0:$PORT -t public"
    }
}
